---
global_pip_to_install: "{{ pup_python_main_global_pip_to_install | default([]) }}"
global_pip_to_uninstall: "{{ pup_python_main_global_pip_to_uninstall | default([]) }}"
virtualenv_path: "{{ pup_python_main_virtualenv_path | default('~/.venv') }}"
virtualenv_pip_to_install: "{{ pup_python_main_virtualenv_pip_to_install | default([]) }}"
virtualenv_pip_to_uninstall: "{{ pup_python_main_virtualenv_pip_to_uninstall | default([]) }}"

dotfile: "{{ pup_python_main_dotfile | default('~/.bashrc') }}"
